โมดูลของระบบสั่งงานด้วยเสียง (เลิกใช้แล้ว เปลี่ยนไปควบคุมด้วย Numpad แทน)

เก็บไว้เผื่อวันหลังอยากได้กลับมา ไม่ได้ถูกเรียกใช้จากโปรแกรมแล้ว ลบทิ้งได้เลย
ถ้าอยากใช้อีก ต้องย้ายกลับเข้า src/lineupmaster/ แล้วต่อกลับใน app.py
  stt.py      - faster-whisper + preload CUDA DLL + fallback GPU->CPU
  audio.py    - อัดไมค์ระหว่างกดปุ่มค้าง
  matcher.py  - ข้อความ -> tag -> จัดอันดับผลลัพธ์
